#include <iostream>
#include <unordered_map>
#include <string>

/*
Gets:
user (optional)

Returns:
[{id, title, content, user}]
ERROR
*/

int main() {
    std::unordered_map<int, std::string, std::string, std::string> posts[4];
    return 0;
}